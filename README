config.ini syntax:

mode=oob
host=
username=
password=
refresh_interval=0
remember_cred=0


config.ini path:
~/.config/ipmi_tui/config.ini


compile with:
gcc main.c -lncursesw -o ipmi_tui

gcc main.c \
-I$(brew --prefix ncurses)/include \
-L$(brew --prefix ncurses)/lib \
-lncursesw \
-o ipmi_tui

